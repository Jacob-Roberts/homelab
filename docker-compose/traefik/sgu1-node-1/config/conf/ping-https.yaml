http:
  routers:
    ping-http:
      rule: "Host(`whoami.sgu1.jakerob.pro`) && PathPrefix(`/ping`)"
      service: "ping@internal"
      entryPoints:
        - web
    ping-https:
      rule: "Host(`whoami.sgu1.jakerob.pro`) && PathPrefix(`/ping`)"
      service: "ping@internal"
      entryPoints:
        - websecure
      tls: {}
