http:
  services:
    lhr1-tunnel:
      loadBalancer:
        servers:
          - url: http://100.89.27.79

  routers:
    whoami-lhr1-http:
      entrypoints: web
      rule: "Host(`whoami-tunnel.jakerob.pro`)"
      middlewares:
        - websecure-redirect-permanent@file
      service: lhr1-tunnel
    whoami-lhr1-https:
      entrypoints: websecure
      rule: "Host(`whoami-tunnel.jakerob.pro`)"
      service: lhr1-tunnel
      tls:
        certresolver: production
    
    wordpress-personal-lhr1-http:
      entrypoints: web
      rule: "Host(`wordpress.jakerob.pro`)"
      middlewares:
        - websecure-redirect-permanent@file
      service: lhr1-tunnel
    wordpress-personal-lhr1-https:
      entrypoints: websecure
      rule: "Host(`wordpress.jakerob.pro`)"
      service: lhr1-tunnel
      tls:
        certresolver: production
